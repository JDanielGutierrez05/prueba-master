{
  "name": "test2-backend",
  "main": "index.js",
  "scripts": {
    "deploy-backend": "cd serverless && sls deploy",
    "remove-backend": "cd serverless && sls remove"
  },
  "devDependencies": {
    "@aws-sdk/client-secrets-manager": "^3.592.0"
  }
}
